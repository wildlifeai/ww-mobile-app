/**
 * Layout Constants
 * 
 * Shared layout values used across the application.
 */

/**
 * Bottom padding for ScrollView content in authentication screens.
 * This extra padding ensures the keyboard doesn't cover input fields on Android
 * when used with CustomKeyboardAvoidingView.
 */
export const KEYBOARD_AVOID_PADDING = 300;
